/* os.js is intentionally kept as the single guest-OS file. */
(function () {
  'use strict';
  /* This marker is used by the inline OS implementation below when adding
     browser-only AI features. API keys remain website settings, never calculator state. */
  window.CASIO_OS_SINGLE_FILE = true;
})();
